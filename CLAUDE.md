# CLAUDE.md

## Formatting & Linting

Run `bun fmt` to fix formatting issues.
Run `bun lint` to fix linting issues.

**Important:** At the end of every conversation (before your final response), run `bun fmt` and `bun lint` to ensure all files are properly formatted and linted. Fix any issues found before finishing.
